/** Versión del esquema de dominio Planning BC (persistencia y meta). */
export const PLANNING_DOMAIN_SCHEMA_VERSION = 1;

export const PLANNING_DOMAIN_SCHEMA_META_KEY = "planning_domain_schema_version";

/** Clave meta opcional en `servicios.referencia` para vínculo expedición ↔ obligación. */
export const TRANSPORT_OBLIGATION_ID_META_KEY = "transport_obligation_id";
