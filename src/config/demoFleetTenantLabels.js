/**
 * Etiquetas de flota conocidas en DEMO (seed scripts/seed-demo.sql).
 * Solo lectura UI cuando RLS impide SELECT en `empresas` al conductor.
 */
export const DEMO_FLEET_TENANT_LABELS = Object.freeze({
  "a0000000-0000-4000-8000-000000000001": Object.freeze({
    nombre: "Cuaderno Demo QA",
    logo_url: null,
  }),
});
